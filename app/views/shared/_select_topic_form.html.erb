<%# <%= simple_form_for :search, method: :get do |f| %>
  <%# <div class="d-flex"> %>
    <%# <%= f.input :topic , collection: Topic.all, label: false, include_blank: false %>
    <%# <%= f.button :submit, 'Search', class: 'search-button' %>
  <%# </div> %>

<%= simple_form_for :search, method: :get, html: { class: "form-group"} do |f| %>
    <%= f.input :topic , collection: Topic.all, label: false, include_blank: false, input_html: {class: 'main-dropdown-search' } %>
  <%= f.button :submit, 'Search', class: 'search-button' %>
<% end %>
