<div class="container">
  <%= link_to 'Add to favorites', topic_favorite_topics_path(@topic.id), method: :post %>

  <h1><%= @topic.title %></h1>

  <%# <p>APi</p>
  <p>APi</p>
  <p>APi</p>
  <p>APi</p>
  <p>APi</p> %>

  <div class="cards">
    <%= render 'shared/article-card' %>
  </div>

  <%# <%= link_to 'Back', root_path, class: 'btn btn-secondary' %>

  <ul>
    <% @recent_search.each do |tweet| %>
      <li><%= tweet['text'] %></li>
    <% end %>
  </ul>

  <%= link_to 'Back', root_path, class: 'btn btn-secondary' %>
</div>
